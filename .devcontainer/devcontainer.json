{
    "name": "cwa-admin-backend",
    "dockerComposeFile": "../docker-compose-workspace.yml",
    "service": "workspace",
    "workspaceFolder": "/app",
    "mounts": ["type=bind,source=/home/${localEnv:USER}/.ssh,target=/root/.ssh,readonly"],
    "customizations": {
        "vscode": {
            "extensions": ["ms-azuretools.vscode-azureappservice", "esbenp.prettier-vscode"]
        }
    }
}
